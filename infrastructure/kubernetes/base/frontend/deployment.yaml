apiVersion: apps/v1
kind: Deployment
metadata:
  name: firefusion-frontend
  namespace: firefusion
  labels:
    app.kubernetes.io/name: firefusion-frontend
    app.kubernetes.io/part-of: firefusion-platform

spec:
  replicas: 2

  selector:
    matchLabels:
      app.kubernetes.io/name: firefusion-frontend

  template:
    metadata:
      labels:
        app.kubernetes.io/name: firefusion-frontend
        app.kubernetes.io/part-of: firefusion-platform

    spec:
      automountServiceAccountToken: false

      securityContext:
        runAsNonRoot: true
        runAsUser: 101
        runAsGroup: 101
        fsGroup: 101
        seccompProfile:
          type: RuntimeDefault

      containers:
        - name: frontend
          image: docker.io/ashanin24x7/firefusion-frontend:1a8a888
          imagePullPolicy: IfNotPresent

          ports:
            - name: http
              containerPort: 8080
              protocol: TCP

          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi

          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL

          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3

          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 3

          volumeMounts:
            - name: nginx-cache
              mountPath: /var/cache/nginx
            - name: nginx-run
              mountPath: /var/run

      volumes:
        - name: nginx-cache
          emptyDir: {}
        - name: nginx-run
          emptyDir: {}